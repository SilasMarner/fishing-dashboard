apiVersion: 1
datasources:
  - name: FishLog
    type: frser-sqlite-datasource
    uid: fish-sqlite
    access: proxy
    editable: true
    jsonData:
      # Path inside the Grafana container — mount your FISHING_DATA_DIR to /fishing
      path: /fishing/fish_log.db
